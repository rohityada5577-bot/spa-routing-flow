
Mini Web App – User Directory and Features
Description

This repository contains a series of mini web app features, including:

User Directory: Fetches user data from a public API and displays it in a responsive grid layout with search, filter, and pagination/infinite scroll functionalities.

Color Theme Switcher: A toggle switch to switch between light and dark modes.

Form Input Validation: A form with real-time validation feedback for fields like name, email, and password.

Static Profile Card: A static profile card design with name, photo, and short bio.

Accordion Menu: A collapsible menu with multiple sections that can be expanded or collapsed.

Each feature is implemented using HTML, CSS, and JavaScript.

Features Implemented
1. User Directory

Fetches user data from a public API (e.g., https://reqres.in
 or JSONPlaceholder
).

Displays user cards in a responsive grid layout.

Includes search and filter functionality.

Implements pagination or infinite scroll to load more users.

2. Color Theme Switcher

Implements a light/dark mode toggle.

Uses CSS and JavaScript to switch themes.

3. Form Input Validation

A form with fields such as name, email, and password.

Real-time validation feedback is provided for the fields, showing whether the user input is valid or not.

4. Static Profile Card

A profile card layout featuring:

Name

Placeholder photo

Short bio

Implemented purely with HTML and CSS.

5. Accordion Menu

A collapsible menu with multiple sections.

Allows one or multiple sections to be expanded or collapsed.

Installation

To run the project on your local machine, follow these steps:

1. Clone the Repository

Clone this repository to your local machine using the following command:

git clone <repository-url>

2. Navigate to the Project Folder

Open your terminal or command prompt and navigate to the project directory:

cd <project-folder>

3. Open the Project

Open the project in your preferred code editor (e.g., Visual Studio Code, Sublime Text, etc.).

Alternatively, if you want to view the project in a browser, simply open the index.html file.

Usage

After cloning and opening the project:

The User Directory will display a grid of user cards fetched from a public API.

You can filter users by name and switch between pages using the pagination controls.

The Color Theme Switcher allows you to toggle between light and dark themes using the button in the top right corner.

The Form Input Validation will provide real-time feedback as you type in the form fields.

The Static Profile Card displays a simple, styled card with a name, photo, and bio.

The Accordion Menu has sections that can be expanded or collapsed when clicked.

Live Demo

You can view the live demo of this project at:

Link to live demo

Technologies Used

HTML: Markup for structure.

CSS: Styling the components, including layout, color themes, and form validation.

JavaScript: Adding interactivity such as theme switching, form validation, search/filtering, and accordion functionality.

Public API: Used for fetching user data (e.g., Reqres
).

Folder Structure
/project-folder
  /assets
    /images    # Placeholder images for profile cards
  index.html     # Main HTML file
  style.css      # CSS styles for the project
  app.js         # JavaScript logic for functionality (theme switching, form validation, etc.)

Setup Instructions

Clone the repository.

Open the index.html file in your browser.

Optionally, open the project in a code editor to make changes or add new features.

Contributing

Feel free to fork this repository and submit issues or pull requests. If you have any suggestions or bug fixes, please open an issue and contribute to this project.

License

This project is open-source and available under the MIT License.
